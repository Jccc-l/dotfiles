// 雷达是否保持玩家居中
cl_radar_always_centered						0;

// 雷达HUD大小
cl_hud_radar_scale								1;

// 雷达缩放倍率0.3
cl_radar_scale									0.3;

// 雷达旋转(0--否，1--是)
cl_radar_rotate									1;

// 雷达HUD地图半透明(0--否，1--是)
cl_hud_radar_map_additive						0;

// 雷达HUD不透明度
cl_hud_radar_background_alpha					0.6;

// 雷达地图切换缩放(0.25~1)
cl_radar_scale_alternate						0.4;

// 切换计分板时，雷达外观(0--圆形保持原样，1--方形固定地图)
cl_radar_square_with_scoreboard					1;
